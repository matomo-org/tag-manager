<div
        vue-entry="TagManager.TrackingCodeReactPage"
        current-action="{{ action|json_encode }}"
        show-container-row="{{ showContainerRow|json_encode }}"
        additional-step-content="{{ additionalStepContent|json_encode }}"
>
</div>